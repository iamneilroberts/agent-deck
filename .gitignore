node_modules/
dist/
*.log
.DS_Store
# Raw, unsanitized protocol captures — never commit these.
spikes/**/captured/raw-*.jsonl
# Local env / secrets
.env
.env.*
.dev.vars

# Local SQLite data (apps/server's EventStore file, when not overridden via AGENTDECK_DB_PATH)
*.sqlite3
