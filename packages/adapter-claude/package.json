{
  "name": "@agentdeck/adapter-claude",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Real AgentAdapter for Claude, driving `claude` headless via @anthropic-ai/claude-agent-sdk (query() stream + canUseTool approvals + resume).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentdeck/shared": "*",
    "@anthropic-ai/claude-agent-sdk": "^0.3.207"
  }
}
